# 🏗️ الهيكل الكامل لموديول الطبيب - Complete Doctor Module Structure

**التاريخ:** 2025-01-17  
**الهدف:** هيكل احترافي شامل ومنظم لموديول الطبيب

---

## 📊 التصنيف الشامل | Complete Categorization

### 1️⃣ **Dashboard & Overview** | لوحة التحكم والنظرة العامة

#### الصفحة الرئيسية: `/dashboard/doctor`
**المكونات:**
- 📊 **Widgets & Cards:**
  - إحصائيات اليوم (مواعيد، مرضى، جلسات)
  - الإشعارات العاجلة
  - المهام المعلقة
  - الرسوم البيانية (جلسات أسبوعية، إيرادات شهرية)
  - حالة العيادة (مفتوحة/مغلقة)
  
- 📈 **Charts & Analytics:**
  - عدد الجلسات (أسبوع/شهر)
  - معدل الحضور
  - الإيرادات
  - توزيع المرضى حسب الحالة
  - ساعات الذروة
  
- 🔔 **Notifications & Alerts:**
  - مواعيد قادمة
  - مهام معلقة
  - تنبيهات مهمة
  - رسائل جديدة

**API Endpoints:**
- `GET /api/doctor/dashboard/stats` - إحصائيات سريعة
- `GET /api/doctor/dashboard/notifications` - الإشعارات
- `GET /api/doctor/dashboard/charts` - بيانات الرسوم البيانية

---

### 2️⃣ **Core Pages** | الصفحات الأساسية والتعريفية

#### 2.1 ملف المريض | Patient Profile
**الصفحة:** `/dashboard/doctor/patients/[id]`
**المكونات:**
- معلومات المريض الأساسية
- السجل الطبي الكامل
- الجلسات السابقة
- خطط العلاج
- الوصفات الطبية
- نتائج الفحوصات
- العلامات الحيوية
- التأمين

**API:**
- `GET /api/doctor/patients/[id]` - معلومات المريض
- `GET /api/doctor/patients/[id]/medical-history` - السجل الطبي
- `GET /api/doctor/patients/[id]/quick-stats` - إحصائيات سريعة

#### 2.2 قائمة المرضى | Patients List
**الصفحة:** `/dashboard/doctor/patients`
**المكونات:**
- قائمة مرضى الطبيب
- بحث متقدم
- فلترة (نشط، معطل، جديد)
- ترتيب حسب (الاسم، آخر زيارة، الحالة)

**API:**
- `GET /api/doctor/patients` - قائمة المرضى
- `GET /api/doctor/search` - بحث متقدم

---

### 3️⃣ **Daily Operations** | العمليات والمهام اليومية

#### 3.1 الجلسات | Sessions
**الصفحة:** `/dashboard/doctor/sessions`
**المكونات:**
- قائمة الجلسات (اليوم، الأسبوع، الشهر)
- إنشاء جلسة جديدة
- تعديل جلسة
- إلغاء/تأجيل جلسة
- ملاحظات الجلسة
- ربط بخطة العلاج

**الصفحات الفرعية:**
- `/dashboard/doctor/sessions/new` - جلسة جديدة
- `/dashboard/doctor/sessions/[id]` - تفاصيل الجلسة
- `/dashboard/doctor/sessions/[id]/edit` - تعديل الجلسة

**API:**
- `GET /api/doctor/sessions` - قائمة الجلسات
- `POST /api/doctor/sessions` - إنشاء جلسة
- `GET /api/doctor/sessions/[id]` - تفاصيل الجلسة
- `PUT /api/doctor/sessions/[id]` - تحديث الجلسة
- `DELETE /api/doctor/sessions/[id]` - حذف الجلسة
- `POST /api/doctor/sessions/bulk-manage` - عمليات مجمعة

#### 3.2 المواعيد | Appointments
**الصفحة:** `/dashboard/doctor/appointments`
**المكونات:**
- مواعيد اليوم
- المواعيد القادمة
- تأكيد/إلغاء المواعيد
- إعادة جدولة

**API:**
- `GET /api/doctor/appointments` - قائمة المواعيد
- `PUT /api/doctor/appointments/[id]` - تحديث الموعد

#### 3.3 طابور الاستقبال | Reception Queue
**الصفحة:** `/dashboard/doctor/queue`
**المكونات:**
- المرضى في الانتظار
- تأكيد وصول المريض
- بدء الجلسة
- حالة العيادة (مفتوحة/مغلقة)

**API:**
- `GET /api/doctor/patient-visit` - زيارة المريض الحالية
- `PUT /api/doctor/patient-visit/[id]` - تحديث حالة الزيارة

---

### 4️⃣ **Treatment & Planning** | العلاج والتخطيط

#### 4.1 خطط العلاج | Treatment Plans
**الصفحة:** `/dashboard/doctor/treatment-plans`
**المكونات:**
- قائمة خطط العلاج
- إنشاء خطة جديدة
- تعديل خطة
- تتبع التقدم
- إكمال/إيقاف خطة

**الصفحات الفرعية:**
- `/dashboard/doctor/treatment-plans/new` - خطة جديدة
- `/dashboard/doctor/treatment-plans/[id]` - تفاصيل الخطة
- `/dashboard/doctor/treatment-plans/[id]/progress` - تتبع التقدم

**API:**
- `GET /api/doctor/treatment-plans` - قائمة الخطط
- `POST /api/doctor/treatment-plans` - إنشاء خطة
- `GET /api/doctor/treatment-plans/[id]` - تفاصيل الخطة
- `PUT /api/doctor/treatment-plans/[id]` - تحديث الخطة
- `GET /api/doctor/progress-tracking` - تتبع التقدم

#### 4.2 السجلات الطبية | Medical Records
**الصفحة:** `/dashboard/doctor/medical-records`
**المكونات:**
- قائمة السجلات الطبية
- إنشاء سجل جديد
- البحث في السجلات
- تصدير السجلات

**الصفحات الفرعية:**
- `/dashboard/doctor/patients/[id]/new-record` - سجل جديد

**API:**
- `GET /api/doctor/medical-records` - قائمة السجلات
- `POST /api/doctor/medical-records` - إنشاء سجل

---

### 5️⃣ **Schedule & Time Management** | الجدولة وإدارة الوقت

#### 5.1 الجدول الزمني | Schedule
**الصفحة:** `/dashboard/doctor/schedule`
**المكونات:**
- عرض أسبوعي/شهري
- ساعات العمل
- إجازات
- مواعيد محجوزة
- أوقات متاحة

**الصفحات الفرعية:**
- `/dashboard/doctor/schedule/weekly` - عرض أسبوعي
- `/dashboard/doctor/schedule/monthly` - عرض شهري
- `/dashboard/doctor/schedule/availability` - إدارة الأوقات المتاحة

**API:**
- `GET /api/doctor/schedule` - الجدول الزمني
- `POST /api/doctor/schedule` - إنشاء/تحديث الجدول
- `GET /api/doctor/schedule/[id]` - تفاصيل الجدول
- `PUT /api/doctor/schedule/[id]` - تحديث الجدول

#### 5.2 تخطيط أوقات العمل | Working Hours Planning
**الصفحة:** `/dashboard/doctor/schedule/working-hours`
**المكونات:**
- تخطيط ساعات العمل الأسبوعية
- تخطيط ساعات العمل الشهرية
- تحديد الإجازات
- تحديد الأوقات المتاحة
- قوالب الجداول (حفظ واستخدام)

**API:**
- `GET /api/doctor/schedule/working-hours` - ساعات العمل
- `POST /api/doctor/schedule/working-hours` - حفظ ساعات العمل
- `GET /api/doctor/schedule/templates` - قوالب الجداول

---

### 6️⃣ **Remote Sessions & Video** | الجلسات عن بعد

#### 6.1 الجلسات المرئية | Video Sessions
**الصفحة:** `/dashboard/doctor/video-sessions`
**المكونات:**
- قائمة الجلسات المرئية
- إنشاء جلسة مرئية
- إدارة الجلسات النشطة
- تسجيلات الجلسات
- إعدادات الجلسات المرئية

**الصفحات الفرعية:**
- `/dashboard/doctor/video-sessions/new` - جلسة مرئية جديدة
- `/dashboard/doctor/video-sessions/[id]` - تفاصيل الجلسة
- `/dashboard/doctor/video-sessions/settings` - إعدادات الجلسات

**API:**
- `GET /api/doctor/video-sessions` - قائمة الجلسات المرئية
- `POST /api/doctor/video-sessions` - إنشاء جلسة مرئية
- `GET /api/doctor/video-sessions/[id]` - تفاصيل الجلسة
- `PUT /api/doctor/video-sessions/[id]` - تحديث الجلسة
- `POST /api/doctor/video-sessions/[id]/start` - بدء الجلسة
- `POST /api/doctor/video-sessions/[id]/end` - إنهاء الجلسة

#### 6.2 إعدادات الجلسات المرئية | Video Settings
**الصفحة:** `/dashboard/doctor/video-sessions/settings`
**المكونات:**
- اختيار منصة الفيديو (Zoom, Google Meet, etc.)
- إعدادات التسجيل
- إعدادات الجودة
- إعدادات الأمان
- ربط مع التقويم

**API:**
- `GET /api/system/config/video-recording` - إعدادات التسجيل
- `PUT /api/system/config/video-recording` - تحديث الإعدادات

---

### 7️⃣ **Automation & Templates** | الأتمتة والقوالب

#### 7.1 قوالب الملاحظات | Notes Templates
**الصفحة:** `/dashboard/doctor/templates`
**المكونات:**
- قائمة القوالب
- إنشاء قالب جديد
- تعديل/حذف قالب
- استخدام قالب في جلسة
- مشاركة القوالب

**API:**
- `GET /api/doctor/notes-templates` - قائمة القوالب
- `POST /api/doctor/notes-templates` - إنشاء قالب
- `GET /api/doctor/notes-templates/[id]` - تفاصيل القالب
- `PUT /api/doctor/notes-templates/[id]` - تحديث القالب
- `DELETE /api/doctor/notes-templates/[id]` - حذف القالب

#### 7.2 التوثيق التلقائي | Auto-Documentation
**الصفحة:** `/dashboard/doctor/auto-documentation`
**المكونات:**
- تفعيل/تعطيل التوثيق التلقائي
- إعدادات التحويل الصوتي
- قوالب التلخيص التلقائي
- مراجعة وتعديل الملاحظات التلقائية

**API:**
- `POST /api/doctor/sessions/[id]/record` - بدء التسجيل
- `POST /api/doctor/sessions/[id]/transcribe` - تحويل الصوت
- `POST /api/doctor/sessions/[id]/auto-document` - توليد الملاحظات تلقائياً

---

### 8️⃣ **Reports & Analytics** | التقارير والتحليلات

#### 8.1 التقارير | Reports
**الصفحة:** `/dashboard/doctor/reports`
**المكونات:**
- تقارير الجلسات
- تقارير المرضى
- تقارير الإيرادات
- تقارير الأداء
- تقارير مخصصة

**الصفحات الفرعية:**
- `/dashboard/doctor/reports/sessions` - تقارير الجلسات
- `/dashboard/doctor/reports/patients` - تقارير المرضى
- `/dashboard/doctor/reports/revenue` - تقارير الإيرادات
- `/dashboard/doctor/reports/performance` - تقارير الأداء

**API:**
- `GET /api/doctor/analytics/performance` - إحصائيات الأداء
- `GET /api/doctor/analytics/patients` - إحصائيات المرضى
- `GET /api/doctor/analytics/sessions` - إحصائيات الجلسات
- `GET /api/doctor/analytics/revenue` - إحصائيات الإيرادات
- `GET /api/doctor/export` - تصدير البيانات

#### 8.2 الاستعلامات | Queries
**الصفحة:** `/dashboard/doctor/queries`
**المكونات:**
- بحث متقدم
- استعلامات محفوظة
- فلترة متقدمة
- تصدير النتائج

**API:**
- `GET /api/doctor/search` - بحث متقدم

---

### 9️⃣ **Settings & Configuration** | الإعدادات والكونفيجريشن

#### 9.1 إعدادات الطبيب | Doctor Settings
**الصفحة:** `/dashboard/doctor/settings`
**المكونات:**
- معلومات الطبيب الشخصية
- التخصص والخبرة
- الشهادات والتراخيص
- اللغة والاتصالات
- إعدادات الإشعارات
- إعدادات الأمان

**API:**
- `GET /api/doctor/profile` - بروفايل الطبيب
- `PUT /api/doctor/profile` - تحديث البروفايل

#### 9.2 إعدادات العيادة | Clinic Configuration
**الصفحة:** `/dashboard/doctor/settings/clinic`
**المكونات:**
- فتح/إغلاق العيادة
- ساعات العمل
- أيام الإجازة
- السعة اليومية
- إعدادات المواعيد
- رسوم الاستشارة

**API:**
- `GET /api/doctor/schedule` - إعدادات الجدول
- `PUT /api/doctor/schedule` - تحديث الجدول
- `POST /api/doctor/clinic/open` - فتح العيادة
- `POST /api/doctor/clinic/close` - إغلاق العيادة

#### 9.3 إعدادات الأتمتة | Automation Settings
**الصفحة:** `/dashboard/doctor/settings/automation`
**المكونات:**
- إعدادات الإشعارات التلقائية
- إعدادات التذكيرات
- إعدادات التوثيق التلقائي
- إعدادات التقارير التلقائية
- قواعد العمل التلقائية

---

### 🔟 **Additional Features** | ميزات إضافية

#### 10.1 التعاون في الحالات | Case Collaboration
**الصفحة:** `/dashboard/doctor/collaboration`
**المكونات:**
- طلب استشارة من طبيب آخر
- مشاركة حالة مع طبيب
- مراجعة الحالات المشتركة
- مناقشة الحالات

**API:**
- `GET /api/doctor/case-collaboration` - قائمة التعاون
- `POST /api/doctor/case-collaboration` - طلب استشارة
- `GET /api/doctor/case-collaboration/[id]` - تفاصيل الاستشارة
- `POST /api/doctor/case-collaboration/[id]/comments` - إضافة تعليق

#### 10.2 كشف المخاطر | Risk Detection
**الصفحة:** `/dashboard/doctor/risk-detection`
**المكونات:**
- تنبيهات المخاطر
- حالات عالية الخطورة
- متابعة الحالات الحرجة

**API:**
- `GET /api/doctor/risk-detection` - كشف المخاطر

#### 10.3 التسجيلات | Recordings
**الصفحة:** `/dashboard/doctor/recordings`
**المكونات:**
- قائمة التسجيلات
- تشغيل التسجيلات
- تحميل التسجيلات
- حذف التسجيلات

**API:**
- `GET /api/doctor/recordings` - قائمة التسجيلات
- `GET /api/doctor/recordings/[id]` - تفاصيل التسجيل

#### 10.4 تكامل Slack | Slack Integration
**الصفحة:** `/dashboard/doctor/slack`
**المكونات:**
- قنوات Slack للمرضى
- إرسال رسائل
- إدارة القنوات

**API:**
- `GET /api/doctor/slack` - قائمة القنوات
- `POST /api/doctor/slack/[patient_id]` - إنشاء قناة

---

## 📋 هيكل السايدبار الاحترافي | Professional Sidebar Structure

### التصنيف المقترح:

```
🏠 الرئيسية
├── 📊 لوحة التحكم (Dashboard)
└── 🔔 الإشعارات

👥 المرضى
├── 📋 قائمة المرضى
├── 👤 ملف المريض
└── ➕ مريض جديد

📅 الجدولة
├── 📆 الجدول الزمني
├── ⏰ تخطيط أوقات العمل
├── 📝 المواعيد
└── 🎫 طابور الاستقبال

💼 الجلسات
├── 📋 قائمة الجلسات
├── ➕ جلسة جديدة
├── 🎥 الجلسات المرئية
└── 🎙️ التسجيلات

📋 العلاج والتخطيط
├── 📊 خطط العلاج
├── 📝 السجلات الطبية
└── 📈 تتبع التقدم

🤖 الأتمتة
├── 📄 قوالب الملاحظات
├── 🤖 التوثيق التلقائي
└── ⚙️ إعدادات الأتمتة

📊 التقارير والتحليلات
├── 📈 الأداء
├── 👥 المرضى
├── 💰 الإيرادات
└── 📋 التقارير المخصصة

⚙️ الإعدادات
├── 👤 بروفايلي
├── 🏥 إعدادات العيادة
├── 🎥 إعدادات الجلسات المرئية
└── 🔔 الإشعارات
```

---

## 🎯 أولويات التنفيذ | Implementation Priorities

### Phase 1: الأساسيات (أسبوع 1-2)
1. ✅ Dashboard غني بالمعلومات
2. ✅ صفحات المرضى الأساسية
3. ✅ صفحات الجلسات
4. ✅ صفحات خطط العلاج

### Phase 2: الجدولة والأتمتة (أسبوع 3-4)
1. ✅ الجدول الزمني
2. ✅ تخطيط أوقات العمل
3. ✅ قوالب الملاحظات
4. ✅ التوثيق التلقائي

### Phase 3: الجلسات المرئية (أسبوع 5)
1. ✅ الجلسات المرئية
2. ✅ إعدادات الجلسات المرئية
3. ✅ التسجيلات

### Phase 4: التقارير والإعدادات (أسبوع 6)
1. ✅ التقارير والتحليلات
2. ✅ إعدادات شاملة
3. ✅ إعدادات العيادة

---

## 📝 ملاحظات التنفيذ | Implementation Notes

1. **التكامل:** جميع الصفحات يجب أن تكون متكاملة مع بعضها
2. **الاتساق:** استخدام نفس التصميم والأنماط في جميع الصفحات
3. **الأداء:** تحسين الاستعلامات والتحميل
4. **الأمان:** تطبيق RLS والصلاحيات بشكل صحيح
5. **التجربة:** واجهة سهلة ومريحة للطبيب

---

**تم إعداد الهيكل بواسطة:** AI Assistant  
**التاريخ:** 2025-01-17  
**الإصدار:** 1.0

