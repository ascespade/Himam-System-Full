# 🏗️ الهيكل الكامل لموديول الاستقبال - Complete Reception Module Structure

**التاريخ:** 2025-01-17  
**الهدف:** موديول استقبال احترافي شامل - أول خطوة في النظام قبل موديول الطبيب  
**الحالة:** ⭐⭐⭐⭐⭐ (5/5) - احترافي كامل

---

## 📊 التصنيف الشامل | Complete Categorization

### 1️⃣ **Dashboard & Overview** | لوحة التحكم والنظرة العامة

#### الصفحة الرئيسية: `/dashboard/reception`
**المكونات:**
- 📊 **Widgets & Cards:**
  - إحصائيات اليوم (مرضى جدد، مواعيد، طابور)
  - المرضى في الانتظار (Queue)
  - المواعيد القادمة
  - الإشعارات العاجلة
  - الرسوم البيانية (إحصائيات يومية/أسبوعية)
  
- 📈 **Charts & Analytics:**
  - عدد المرضى المسجلين (يوم/أسبوع/شهر)
  - عدد المواعيد المحجوزة
  - معدل الحضور
  - توزيع المواعيد حسب الطبيب
  - ساعات الذروة
  
- 🔔 **Notifications & Alerts:**
  - مواعيد قادمة (خلال ساعة)
  - مرضى في الانتظار
  - تنبيهات مهمة
  - رسائل جديدة

**API Endpoints:**
- `GET /api/reception/dashboard/stats` - إحصائيات سريعة
- `GET /api/reception/dashboard/notifications` - الإشعارات
- `GET /api/reception/dashboard/charts` - بيانات الرسوم البيانية

---

### 2️⃣ **Patient Registration** | تسجيل المرضى

#### 2.1 تسجيل مريض جديد
**الصفحة:** `/dashboard/reception/patients/new`
**المكونات:**
- نموذج تسجيل شامل:
  - المعلومات الأساسية (الاسم، الهاتف، الجنس، تاريخ الميلاد)
  - معلومات الاتصال (البريد الإلكتروني، العنوان)
  - معلومات التأمين (إن وجدت)
  - معلومات طبية أساسية (فصيلة الدم، الحساسيات، الأمراض المزمنة)
  - معلومات الطوارئ (جهة الاتصال)
  - رفع المستندات (هوية، تأمين)
  
- Features:
  - البحث عن مريض موجود (تجنب التكرار)
  - Validation شامل
  - حفظ تلقائي (Auto-save)
  - طباعة استمارة التسجيل

**API:**
- `POST /api/reception/patients` - تسجيل مريض جديد
- `GET /api/reception/patients/search` - البحث عن مريض
- `POST /api/reception/patients/check-duplicate` - التحقق من التكرار

#### 2.2 قائمة المرضى
**الصفحة:** `/dashboard/reception/patients`
**المكونات:**
- قائمة جميع المرضى
- بحث متقدم (اسم، هاتف، رقم الهوية)
- فلترة (نشط، معطل، جديد)
- ترتيب حسب (الاسم، تاريخ التسجيل، آخر زيارة)
- إجراءات سريعة (عرض، تعديل، حذف، حجز موعد)

**API:**
- `GET /api/reception/patients` - قائمة المرضى
- `GET /api/reception/patients/[id]` - تفاصيل المريض
- `PUT /api/reception/patients/[id]` - تحديث المريض
- `DELETE /api/reception/patients/[id]` - حذف المريض

#### 2.3 ملف المريض
**الصفحة:** `/dashboard/reception/patients/[id]`
**المكونات:**
- معلومات المريض الكاملة
- السجل الطبي (من الطبيب)
- المواعيد السابقة والقادمة
- الفواتير والمدفوعات
- المستندات المرفقة
- إجراءات سريعة (حجز موعد، إضافة للطابور)

**API:**
- `GET /api/reception/patients/[id]/full-profile` - الملف الكامل
- `GET /api/reception/patients/[id]/appointments` - المواعيد
- `GET /api/reception/patients/[id]/billing` - الفواتير

---

### 3️⃣ **Appointment Management** | إدارة المواعيد

#### 3.1 حجز موعد جديد
**الصفحة:** `/dashboard/reception/appointments/new`
**المكونات:**
- اختيار المريض (بحث أو اختيار من القائمة)
- اختيار الطبيب
- اختيار التاريخ والوقت
- اختيار نوع الخدمة
- المدة المتوقعة
- ملاحظات
- إرسال تأكيد عبر WhatsApp/SMS

**API:**
- `POST /api/reception/appointments` - حجز موعد
- `GET /api/reception/appointments/available-slots` - الأوقات المتاحة
- `POST /api/reception/appointments/send-confirmation` - إرسال تأكيد

#### 3.2 قائمة المواعيد
**الصفحة:** `/dashboard/reception/appointments`
**المكونات:**
- عرض المواعيد (اليوم، الأسبوع، الشهر)
- فلترة حسب (الطبيب، الحالة، التاريخ)
- إجراءات (تأكيد، إلغاء، إعادة جدولة)
- عرض تقويم
- إشعارات المواعيد القادمة

**API:**
- `GET /api/reception/appointments` - قائمة المواعيد
- `PUT /api/reception/appointments/[id]` - تحديث الموعد
- `DELETE /api/reception/appointments/[id]` - إلغاء الموعد
- `POST /api/reception/appointments/[id]/reschedule` - إعادة جدولة

#### 3.3 التقويم
**الصفحة:** `/dashboard/reception/calendar`
**المكونات:**
- عرض تقويم شهري/أسبوعي/يومي
- عرض جميع المواعيد
- إضافة موعد مباشرة من التقويم
- سحب وإفلات لإعادة الجدولة
- ألوان مختلفة حسب الطبيب/الحالة

---

### 4️⃣ **Queue Management** | إدارة الطابور

#### 4.1 شاشة الطابور الرئيسية
**الصفحة:** `/dashboard/reception/queue`
**المكونات:**
- عرض المرضى في الطابور (real-time)
- إحصائيات سريعة (في الانتظار، قيد المعالجة، مكتمل)
- فلترة حسب الحالة
- بحث سريع
- إجراءات سريعة:
  - إضافة مريض للطابور
  - استدعاء التالي
  - تأكيد وصول المريض
  - إرسال للطبيب
  - إلغاء/حذف

**API:**
- `GET /api/reception/queue` - قائمة الطابور
- `POST /api/reception/queue` - إضافة للطابور
- `PUT /api/reception/queue/[id]` - تحديث حالة
- `POST /api/reception/queue/[id]/confirm-to-doctor` - تأكيد للطبيب
- `DELETE /api/reception/queue/[id]` - حذف من الطابور

#### 4.2 إضافة مريض للطابور
**المكونات:**
- اختيار المريض (بحث سريع)
- اختيار الطبيب (إن لم يكن لديه موعد)
- إضافة ملاحظات
- تحديد الأولوية (عادي، عاجل)
- إضافة تلقائية للطابور

---

### 5️⃣ **Integration with Doctor Module** | التكامل مع موديول الطبيب

#### 5.1 Workflow: من الاستقبال للطبيب
**الخطوات:**
1. **تسجيل المريض** (إن كان جديد)
2. **حجز موعد** (اختياري)
3. **إضافة للطابور** عند الوصول
4. **تأكيد للطبيب** → ينشئ `patient_visit`
5. **الطبيب يستقبل** → يظهر في `PatientContext`
6. **بدء الجلسة** → ينشئ `session`

**API Integration:**
- `POST /api/reception/queue/[id]/confirm-to-doctor` - الربط الرئيسي
- ينشئ `patient_visit` record
- يحدث `reception_queue` status
- يرسل إشعار للطبيب
- يضيف للمريض في `PatientContext` للطبيب

#### 5.2 Real-time Updates
- استخدام Supabase Realtime
- تحديث تلقائي للطابور
- إشعارات فورية للطبيب
- تحديث حالة المريض

---

### 6️⃣ **Reports & Analytics** | التقارير والإحصائيات

#### 6.1 التقارير
**الصفحة:** `/dashboard/reception/reports`
**المكونات:**
- تقرير المرضى الجدد (يوم/أسبوع/شهر)
- تقرير المواعيد
- تقرير الطابور
- تقرير الحضور
- تصدير PDF/Excel

**API:**
- `GET /api/reception/reports/patients` - تقرير المرضى
- `GET /api/reception/reports/appointments` - تقرير المواعيد
- `GET /api/reception/reports/queue` - تقرير الطابور
- `POST /api/reception/reports/export` - تصدير

#### 6.2 Analytics Dashboard
**المكونات:**
- رسوم بيانية (Charts)
- إحصائيات تفصيلية
- تحليل الاتجاهات
- مقارنات (يوم/أسبوع/شهر)

---

### 7️⃣ **Notifications & Alerts** | الإشعارات والتنبيهات

#### 7.1 الإشعارات
- مواعيد قادمة (خلال ساعة)
- مرضى في الانتظار
- تنبيهات مهمة
- رسائل جديدة

#### 7.2 WhatsApp/SMS Integration
- إرسال تأكيد المواعيد
- تذكير بالمواعيد
- إشعارات الوصول

---

## 📁 Structure | الهيكل

```
app/
├── dashboard/
│   └── reception/
│       ├── page.tsx                    # Dashboard الرئيسي
│       ├── patients/
│       │   ├── page.tsx                # قائمة المرضى
│       │   ├── new/
│       │   │   └── page.tsx            # تسجيل مريض جديد
│       │   └── [id]/
│       │       └── page.tsx            # ملف المريض
│       ├── appointments/
│       │   ├── page.tsx                # قائمة المواعيد
│       │   ├── new/
│       │   │   └── page.tsx            # حجز موعد جديد
│       │   └── [id]/
│       │       └── page.tsx            # تفاصيل الموعد
│       ├── queue/
│       │   └── page.tsx                # شاشة الطابور
│       ├── calendar/
│       │   └── page.tsx                # التقويم
│       └── reports/
│           └── page.tsx                # التقارير

app/api/reception/
├── dashboard/
│   ├── stats/
│   │   └── route.ts                    # إحصائيات Dashboard
│   ├── notifications/
│   │   └── route.ts                    # الإشعارات
│   └── charts/
│       └── route.ts                    # بيانات Charts
├── patients/
│   ├── route.ts                        # CRUD المرضى
│   ├── search/
│   │   └── route.ts                    # بحث المرضى
│   ├── check-duplicate/
│   │   └── route.ts                    # التحقق من التكرار
│   └── [id]/
│       ├── route.ts                    # تفاصيل/تحديث/حذف
│       ├── full-profile/
│       │   └── route.ts                # الملف الكامل
│       ├── appointments/
│       │   └── route.ts                # مواعيد المريض
│       └── billing/
│           └── route.ts                 # فواتير المريض
├── appointments/
│   ├── route.ts                        # CRUD المواعيد
│   ├── available-slots/
│   │   └── route.ts                    # الأوقات المتاحة
│   ├── send-confirmation/
│   │   └── route.ts                    # إرسال تأكيد
│   └── [id]/
│       ├── route.ts                    # تفاصيل/تحديث/حذف
│       └── reschedule/
│           └── route.ts                # إعادة جدولة
├── queue/
│   ├── route.ts                        # CRUD الطابور
│   └── [id]/
│       ├── route.ts                    # تحديث/حذف
│       └── confirm-to-doctor/
│           └── route.ts                # تأكيد للطبيب
└── reports/
    ├── patients/
    │   └── route.ts                    # تقرير المرضى
    ├── appointments/
    │   └── route.ts                    # تقرير المواعيد
    ├── queue/
    │   └── route.ts                    # تقرير الطابور
    └── export/
        └── route.ts                    # تصدير
```

---

## 🔄 Workflow | سير العمل

### Workflow 1: تسجيل مريض جديد وحجز موعد
```
1. الاستقبال → /dashboard/reception/patients/new
2. ملء نموذج التسجيل
3. حفظ المريض → POST /api/reception/patients
4. (اختياري) حجز موعد → /dashboard/reception/appointments/new
5. اختيار الطبيب والتاريخ والوقت
6. حفظ الموعد → POST /api/reception/appointments
7. إرسال تأكيد → POST /api/reception/appointments/send-confirmation
```

### Workflow 2: وصول مريض وإرساله للطبيب
```
1. المريض يصل → /dashboard/reception/queue
2. إضافة للطابور → POST /api/reception/queue
   - اختيار المريض
   - اختيار الطبيب (إن لم يكن لديه موعد)
3. عند الدور → POST /api/reception/queue/[id]/confirm-to-doctor
   - ينشئ patient_visit
   - يرسل إشعار للطبيب
   - يضيف للمريض في PatientContext
4. الطبيب يستقبل → /dashboard/doctor/current-patient
5. بدء الجلسة → /dashboard/doctor/sessions/new
```

### Workflow 3: مريض لديه موعد
```
1. المريض يصل في وقت الموعد
2. البحث عن الموعد → /dashboard/reception/appointments
3. إضافة للطابور مع ربط الموعد
4. تأكيد للطبيب → ينشئ patient_visit
5. الطبيب يستقبل ويبدأ الجلسة
```

---

## 🔗 Integration Points | نقاط التكامل

### مع موديول الطبيب:
1. **patient_visits table** - ربط الاستقبال بالطبيب
2. **PatientContext** - سياق المريض في موديول الطبيب
3. **Notifications** - إشعارات للطبيب
4. **Real-time updates** - تحديثات فورية

### مع النظام العام:
1. **WhatsApp Integration** - إرسال تأكيدات وتذكيرات
2. **Billing System** - ربط الفواتير
3. **Calendar Integration** - مزامنة المواعيد
4. **Notifications System** - إشعارات شاملة

---

## ✅ Checklist | قائمة التحقق

### Core Features:
- [x] Dashboard شامل
- [x] تسجيل مرضى جدد
- [x] إدارة المرضى
- [x] حجز المواعيد
- [x] إدارة المواعيد
- [x] إدارة الطابور
- [x] Integration مع موديول الطبيب
- [x] التقارير والإحصائيات
- [x] الإشعارات

### APIs:
- [x] جميع APIs المطلوبة
- [x] Error handling
- [x] Validation
- [x] Authentication & Authorization

### UI/UX:
- [x] تصميم احترافي
- [x] Responsive design
- [x] Real-time updates
- [x] Loading states
- [x] Error handling

---

**تم إعداد الوثيقة بواسطة:** AI Assistant  
**التاريخ:** 2025-01-17  
**الإصدار:** 1.0
